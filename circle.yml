machine:
  java:
    version: oraclejdk8
  post:
    - cd retcon.address.parser
    - ls
    - gradle buildZip
    - cp build/distributions/* $CIRCLE_ARTIFACTS

general:
  artifacts:
    - "build/distributions"